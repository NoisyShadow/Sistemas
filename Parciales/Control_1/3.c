#include <stdio.h>

int main(){
    int inp,np,ip;
    for(getchar inp()){
        if (inp = 0  && inp +2 ){
            inp = np
            ++np;
        }else if (inp = 1 && inp + 2){
            inp = ip
            ++ip;
        }else (inp < 0 ){
            printf("La cantidad de numeros pares introducidos es de:\n",np);
            printf("La cantidad de numeros impares introducidos es de:\n",ip);
        }
    }
    return 0;
}